{
  "name": "@fbltd/dependency-stream-react",
  "version": "0.0.0",
  "description": "Wrapper for DependencyStream for React",
  "scripts": {
    "clearDist": "rm dist -rf || true",
    "clearModules": "rm node_modules -rf || true",
    "clearAll": "npm run clearDist && npm run clearModules",
    "build": "npm run clearAll && npm i && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^19.1.8",
    "typescript": "^5.8.3"
  }
}
